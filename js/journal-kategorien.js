window.JournalKategorien = (() => {
  const el = (id) => document.getElementById(id); let rows = []; let aktuell = null;
  async function init() { el("jkSuche").addEventListener("input", rendern); el("jkNeu").addEventListener("click", neu); el("jkBody").addEventListener("click", aktion); el("jkSpeichern").addEventListener("click", speichern); el("jkAbbrechen").addEventListener("click", schliessen); el("jkSchliessen").addEventListener("click", schliessen); el("jkModal").addEventListener("click", (e) => { if (e.target === el("jkModal")) schliessen(); }); await laden(); }
  async function laden() { try { rows = await JournalKategorienService.laden(); rendern(); } catch (error) { AppFeedback.error(error.message); } }
  function rendern() { const q = el("jkSuche").value.trim().toLocaleLowerCase("de"); el("jkBody").innerHTML = ""; rows.filter((row) => !q || `${row.nr} ${row.bezeichnung}`.toLocaleLowerCase("de").includes(q)).forEach((row) => { const tr = document.createElement("tr"); tr.innerHTML = `<td data-label="Nr.">${row.nr}</td><td data-label="Bezeichnung"></td><td data-label="Aktiv">${row.aktiv ? "Ja" : "Nein"}</td><td data-label="Aktionen" class="action-cell"><button class="action-btn edit-btn" data-action="edit" data-id="${row.id}" title="Bearbeiten" aria-label="Bearbeiten"></button><button class="action-btn delete-btn" data-action="delete" data-id="${row.id}" title="Löschen" aria-label="Löschen"></button></td>`; tr.children[1].textContent = row.bezeichnung; el("jkBody").appendChild(tr); }); BerechtigungService.aktionsrechteAnwenden("journal-kategorien", el("jkBody")); }
  function oeffnen() { el("jkFehler").hidden = true; el("jkModal").style.display = "block"; el("jkModal").setAttribute("aria-hidden", "false"); }
  function schliessen() { el("jkModal").style.display = "none"; el("jkModal").setAttribute("aria-hidden", "true"); aktuell = null; }
  function neu() { aktuell = null; el("jkTitel").textContent = "Kategorie anlegen"; el("jkNr").value = rows.reduce((m, r) => Math.max(m, Number(r.nr)), 0) + 1; el("jkBezeichnung").value = ""; el("jkAktiv").checked = true; oeffnen(); }
  async function aktion(event) { const button = event.target.closest("button[data-id]"); if (!button) return; const row = rows.find((item) => String(item.id) === button.dataset.id); if (!row) return; if (button.dataset.action === "edit") { aktuell = row; el("jkTitel").textContent = "Kategorie bearbeiten"; el("jkNr").value = row.nr; el("jkBezeichnung").value = row.bezeichnung; el("jkAktiv").checked = row.aktiv; return oeffnen(); } if (!confirm(`Kategorie „${row.bezeichnung}“ wirklich löschen?`)) return; try { await JournalKategorienService.loeschen(row.id); await laden(); AppFeedback.success("Kategorie gelöscht."); } catch (error) { AppFeedback.error(error.message); } }
  async function speichern() { const daten = { nr: Number(el("jkNr").value), bezeichnung: el("jkBezeichnung").value.trim(), aktiv: el("jkAktiv").checked }; if (!Number.isInteger(daten.nr) || daten.nr < 1 || !daten.bezeichnung) { el("jkFehler").textContent = "Nr. und Bezeichnung sind erforderlich."; el("jkFehler").hidden = false; return; } el("jkSpeichern").disabled = true; try { if (aktuell) await JournalKategorienService.aendern(aktuell.id, daten); else await JournalKategorienService.anlegen(daten); schliessen(); await laden(); AppFeedback.success("Kategorie gespeichert."); } catch (error) { el("jkFehler").textContent = error.message; el("jkFehler").hidden = false; } finally { el("jkSpeichern").disabled = false; } }
  return { init };
})();
